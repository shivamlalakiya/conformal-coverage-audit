{
  "title": "Conformal quantile convention probes: harnesses for auditing finite-sample validity in conformal-prediction implementations",
  "upload_type": "software",
  "license": "MIT",
  "version": "v1.1.0",
  "creators": [
    {
      "name": "Lalakiya, Shivam"
    }
  ],
  "description": "<p>Measurement harnesses used to audit how Python conformal-prediction implementations resolve the conformal quantile at finite sample sizes.</p><p>A finite-sample distribution-free interval is a statement about an order statistic; for n exchangeable scores the interval built on rank r covers a fresh observation with probability exactly r/(n+1). Quantile APIs accept a level rather than a rank, and each sample-quantile definition maps that level to a position differently. These probes measure which rank each implementation actually lands on, and what coverage that rank delivers.</p><p>Each script validates its closed forms against exact rational arithmetic at import time. A failing self-check aborts the run; several of these checks caught errors in their own author's hand-derived assertions, which is the reason they exist. Two findings were <strong>retracted</strong> after such checks contradicted them, and the probes that adjudicated them are included rather than removed.</p><p><strong>The convention in isolation.</strong></p><ul><li><code>rank_map.py</code> &mdash; the nine Hyndman &amp; Fan sample-quantile definitions plus NumPy's four discontinuous aliases, examined as carriers of a coverage guarantee rather than as estimators: delivered rank, the level that recovers the required rank, and the delivered minimum calibration size beside the theoretical one.</li><li><code>branch_d_check.py</code> &mdash; an uncorrected level versus the required order statistic on identical draws, 200k draws per cell, <code>fractions.Fraction</code> oracle.</li><li><code>convention_probe.py</code> &mdash; structural branch identification, coverage sweeps with paired standard errors, threshold extraction.</li><li><code>fractional_rank.py</code> &mdash; delivered coverage tracked against the virtual index the quantile definition computes, rather than against the branch or the (n+1)/n correction.</li></ul><p><strong>What the interpolated threshold delivers, and where it stops being O(1/n).</strong></p><ul><li><code>tail_law.py</code>, <code>gpd_tail_law.py</code> &mdash; closed forms for the coverage deficit in both extreme-value domains, and their unification: the deficit depends on the tail only through the shape index.</li><li><code>randomized_bound.py</code> &mdash; the valid randomised bound that exists below the deterministic feasibility floor.</li><li><code>horizon_feasibility.py</code> &mdash; the floor scaled by forecast horizon, and a residual-alignment off-by-one in a multi-horizon helper, verified three independent ways.</li><li><code>conditional_severity.py</code> &mdash; group-conditional coverage at a fixed calibration budget.</li></ul><p><strong>Libraries, end to end.</strong></p><ul><li><code>run_sktime_river.py</code> &mdash; sktime <code>ConformalIntervals</code> through <code>predict_interval</code> and river <code>RegressionJackknife</code> through <code>predict_one</code>, with an oracle independent of both.</li><li><code>run_darts.py</code>, <code>run_darts_tighten.py</code>, <code>darts_scoring_path.py</code> &mdash; exact rank arithmetic, paired Monte Carlo, and end-to-end runs through a real <code>ConformalNaiveModel</code>.</li><li><code>verify_statsforecast_rebuttal.py</code> &mdash; window mapping for <code>ConformalSeasonalPool</code>, and the probe that adjudicated and retracted a finding of the author's own.</li></ul><p><strong>Paired coverage on real data.</strong> Two Monash forecasting collections and two OpenML tabular suites. In every arm the library threshold and a required-rank threshold are built from the same scores and the same point prediction, so the difference isolates the convention.</p><ul><li><code>run_real_data.py</code>, <code>run_real_data_darts.py</code>, <code>run_real_data_statsforecast.py</code>, <code>run_real_data_tabular.py</code>, <code>export_series.py</code>, <code>paired_report.py</code>.</li></ul><p><strong>Census, conformance and generalisation.</strong></p><ul><li><code>helper_census.py</code> &mdash; the level-to-rank resolution sites across the audited packages, classified by threshold extraction rather than by reading, and verified on disk.</li><li><code>conformance_suite.py</code>, <code>certificate.py</code>, <code>certificate_suite.py</code> &mdash; a suite that classifies any quantile helper and reports the branch, the rank landed on, the delivered coverage and the smallest n at which the requested level is delivered; plus a minimal certificate showing that fixture choice is decidable rather than a matter of luck.</li><li><code>cross_library.py</code> &mdash; the cross-language comparison executed rather than transferred arithmetically; the ecosystem defaults disagree.</li><li><code>w8_falsification.py</code> &mdash; whether the mechanism generalises beyond conformal prediction to value-at-risk, tolerance bounds and bootstrap intervals. It is present but <em>not</em> dominant in bootstrap intervals.</li></ul><p>Every probe writes a plain-text output file that is committed alongside it; the 28 files in <code>outputs/</code> are the provenance record for every number the accompanying manuscript reports. Upstream reports for every defect claimed, with filing dates, are in <code>DISCLOSURE.md</code>. The research plan, including what has <em>not</em> been established, is in <code>PLAN.md</code>. Third-party library sources are deliberately <strong>not</strong> redistributed; <code>probe-requirements.txt</code> pins the versions instead.</p>",
  "keywords": [
    "conformal prediction",
    "uncertainty quantification",
    "finite-sample coverage",
    "order statistics",
    "prediction intervals",
    "software correctness",
    "reproducibility"
  ],
  "access_right": "open"
}
