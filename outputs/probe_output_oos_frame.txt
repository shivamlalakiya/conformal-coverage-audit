========================================================================================================
OUT-OF-CENSUS READ -- what was looked at outside the ten, and what came back
========================================================================================================
self_check() passed at import: the population here is the download
denominator's own unaudited side, every row carries a file and a line, and the
rows filed as carrying a site are exactly the packages the three out-of-census
runs name in their outputs.

A SITE is one expression taking a nominated miscoverage target to a position
inside a held-out array of scores, arrived at from a public entry point. That is
the census's own wording and it is applied unchanged, which is the only reason
the two populations can be spoken about together.

========================================================================================================
THE READING CORPUS -- what was downloaded and opened, and what became of it
========================================================================================================
The census reports ten libraries. This is the set those ten came out of, which
no output carried before: the directories under ../cp-src, minus the literature
folder, with every non-audited row opened again while this runs.

--------------------------------------------------------------------------------------------------------
AUDITED -- the census's ten   (10)
--------------------------------------------------------------------------------------------------------
  crepes, darts, mapie, neuralforecast, nonconformist, puncc, river, sktime, statsforecast, torchcp
  (the same ten the census output names under 'P2 by library', checked here)

--------------------------------------------------------------------------------------------------------
TURNED AWAY BY THE CRITERION, AND IT STANDS   (2)
--------------------------------------------------------------------------------------------------------
uq360 0.2
    uq360/uq360/algorithms/infinitesimal_jackknife/infinitesimal_jackknife.py:80
        y_lower = np.quantile(y, q=0.5 * self.config['alpha'], axis=0)
    the array it takes a percentile of is built at :76-79 by setting the model's parameters to each
    perturbed draw and predicting again, so it is a percentile across a predictive distribution and
    no rank or finite-sample coverage attaches to it. The same reasoning turns away the CV+ and
    jackknife+ aggregation elsewhere, which quantile cross-fit prediction arrays rather than scores
    anchor now: re-read, matches

venn_abers 1.5.3
    venn_abers/venn_abers-1.5.3/src/venn_abers.py:706
        self.m_parameter = int(np.round(self.epsilon * (len(y_cal) + 1) / 2))
    epsilon does reach an integer index, and the split really is withheld. What gets ordered at :710
    is y_cal itself. Those positions serve only to clip what feeds the isotonic step, and what comes
    back out are multiprobability bounds. There is no miscoverage target anywhere in the path
    anchor now: re-read, matches

--------------------------------------------------------------------------------------------------------
TURNED AWAY ON A READING THAT RUNNING IT OVERTURNED   (1)
--------------------------------------------------------------------------------------------------------
conformal_tights 0.5.0
    conformal_tights/conformal_tights/_xgboost_weighted_quantile.py:13
        def _weighted_quantile(
    set aside on this helper, where XGBoost emits the quantiles and nothing withheld gets ordered.
    Right about the file, wrong about the distribution. Line 255 of the coherent-quantile module
    puts the caller's own level onto level-2 scores from a third partition, and the criterion admits
    that. Reading missed it; execution did not, and the out-of-census arm carries the numbers
    anchor now: re-read, matches

  packages downloaded and read   13
  audited                        10
  turned away, standing          2
  turned away, overturned        1

🛑 The overturned row is the one to read. conformal_tights was set aside on
_xgboost_weighted_quantile.py, which is a gradient-boosted quantile helper and
forms no order statistic of anything held out. That was true of the file and
false of the distribution. Its conformal path puts the caller's own level onto
scores from a third partition, and it took execution to see that -- which is the
same failure of reading the audit reports in ten other packages, occurring here
in the audit's own frame.

========================================================================================================
THE UNAUDITED SIDE -- a different population, and not a subset of the one above
========================================================================================================
population: 12 packages, read off probe_output_install_weight.txt
            (the two names PyPI could not answer for are not in it)

--------------------------------------------------------------------------------------------------------
CARRIES A SITE -- afterwards put through the suite   (4)
  each of these was executed; the rows are in the conformance arm's outputs
--------------------------------------------------------------------------------------------------------
conformal-tights 0.5.0
    src/conformal_tights/_conformal_coherent_quantile_regressor.py:255
        intercept_l2 = np.quantile(y_cqr_l2 - Δŷ_calib_l2_quantiles[:, j], quantile)
    the asked-for coverage reaches numpy unaltered over the level-2 CQR scores; no (n+1) step, and a
    coherence clip then decides what is kept
    anchor now: re-read, matches
    sdist sha256: 346043c85416028bb8af90da239a3ade830fd2343606aff55dc18492558740dd

crepes-weighted 0.1.3
    src/crepes_weighted/base.py:565
        alpha_index = int((1 - confidence) * (len(self.alphas) + 1)) - 1
    a whole-number index into held-out absolute residuals sorted downward; the branch the fork adds
    is a second expression in the same method, at :576
    anchor now: no local copy, digest filed
    sdist sha256: f6691660e255d23d5ccde2fc27298756a6cc2386b2021a437fbd60ce05f0eb47

fortuna 0.2.0
    fortuna/conformal/regression/quantile.py:90
        return jnp.quantile(scores, jnp.ceil((n + 1) * (1 - error)) / n)
    the rank is worked out and then divided by n and passed on as a level, so it goes above 1
    wherever no rank reaches the request; the same line sits in onedim_uncertainty.py:90
    anchor now: no local copy, digest filed
    sdist sha256: 8e21d65958c71fb4e95842a233d5700b5ad7885b0a8f83d92e25f8325c392811

skforecast 0.24.0
    skforecast/preprocessing/_preprocessing.py:2694
        self.correction_factor_[k] = float(np.quantile(conformity_scores, self.nominal_coverage))
    the asked-for coverage becomes the numpy level as it stands, over held-out conformity scores,
    with no (n+1) step and nothing watching n
    anchor now: no local copy, digest filed
    sdist sha256: 46122c7cf14a487501e71f1d27980871d3f8d032d3fa9d3791df8715a6448de1

--------------------------------------------------------------------------------------------------------
CLAIMED A SITE ON THE FIRST READ, LOST IT ON THE SECOND   (2)
  a second reader was told to break the claim, and did
--------------------------------------------------------------------------------------------------------
quantile-forest 1.4.2
    examples/plot_qrf_conformalized_intervals.py:118
        s = np.quantile(conf_scores, np.clip((1 - alpha) * (1 + (1 / (len(y_calib)))), 0, 1))
    the expression is genuine and it is in examples/, which the census rules out for not being
    public API; the estimator's own predict(quantiles=) weighs training responses pooled from
    leaves, so no held-out array is ranked
    anchor now: no local copy

venn-abers 1.5.4
    src/venn_abers.py:708
        self.m_parameter = int(np.floor(self.epsilon * (len(y_cal) + 1) / 2))
    a level does become a whole-number position on a genuinely held-out split, but what gets sorted
    is the calibration response labels; those order statistics only clip what feeds the isotonic
    step and are not what comes back out
    anchor now: held 1.5.3, read 1.5.4: differs at :708
    sdist sha256: a6bf5383e618ce42d0ecb2335bba0d6b104ecbe979041f10019b57572364bc69

--------------------------------------------------------------------------------------------------------
NO SITE   (6)
  reached by reading alone; nothing here measures it
--------------------------------------------------------------------------------------------------------
pytorch-forecasting 1.8.0
    pytorch_forecasting/metrics/base_metrics/_base_metrics.py:133
        y_pred = torch.quantile(
    taken over the network's own predicted sample axis; elsewhere the quantiles are pinball-trained
    heads or a fitted icdf, and nowhere is there a held-out score set
    anchor now: no local copy

uncertainty-toolbox 0.1.1
    uncertainty_toolbox/metrics_calibration.py:626
            quantile_prediction = norm.ppf(quantile).flatten()
    every bound comes off a predicted mean and spread; isotonic recalibration bends the level first,
    but its observed proportions are tallies on a fixed grid rather than sample ranks, and the
    residuals are never put in order
    anchor now: no local copy

gluonts 0.17.0
    src/gluonts/model/forecast.py:494
                sample_idx = int(np.round((self.num_samples - 1) * q))
    a position on sorted Monte Carlo draws from the model; 'conformal', 'nonconformity' and
    'calibrat' occur nowhere in the sdist
    anchor now: no local copy

nixtla 0.8.0
    nixtla/nixtla_client.py:1928
                out = _maybe_add_intervals(out, resp["intervals"])
    a client against a hosted model: the level travels in the request body and the bounds arrive
    already built, so no arithmetic over scores happens on this side
    anchor now: no local copy

prophet 1.4.0
    prophet/forecaster.py:1693
                lower_p = 100 * (1.0 - self.interval_width) / 2
    a percentile down simulated posterior-predictive trajectories, which are drawn from the fitted
    model and are not held-out scores
    anchor now: no local copy, digest filed
    sdist sha256: cf54064e9ad1be56cf4c6fb70f95c7b0fccc095fdb90a53b72407262d423a70d

orbit-ml 1.1.5.1
    orbit/utils/predictions.py:67
                computed_array = np.percentile(v, percentiles, axis=0)
    a percentile across posterior draws; there is no calibration split, and neither 'conformal' nor
    'nonconformity' appears in the source
    anchor now: no local copy, digest filed
    sdist sha256: f5895d4a999eff7d7dc536c84b62bbf73712e6e6d89ed0ecacaa0eedcdb0cb2a

========================================================================================================
THE COUNTS
========================================================================================================
  packages read                 12
  carrying a site               4
  refuted on the second read    2
  no site                       6

no site count is printed here, on purpose: ruling on how many distinct
expressions each package exposes is work this read did not do, and a number
built without it would not stand beside the census's own.

========================================================================================================
WHAT THIS DOES NOT SETTLE
========================================================================================================
The population was written by name and not swept out of the package index, so
a library nobody here would think of is outside it before any rule is applied.
Eight of the twelve rest on reading alone, at one version apiece, and a package
with nothing at the version opened may well have something at another.
