cff-version: 1.2.0
title: "Conformal quantile convention probes: harnesses for auditing finite-sample validity in conformal-prediction implementations"
message: "If you use these probes or their measurements, please cite this artifact."
type: software
authors:
  - family-names: Lalakiya
    given-names: Shivam
    orcid: "https://orcid.org/0009-0000-5110-6540"
    # Affiliation is deliberately omitted: leaving it out avoids implying
    # institutional endorsement of an independent research artifact.
license: MIT
# all-versions (concept) DOI — always resolves to the latest release
doi: 10.5281/zenodo.21799203
identifiers:
  - type: doi
    value: 10.5281/zenodo.21799203
    description: "All versions"
  - type: doi
    value: 10.5281/zenodo.21799204
    description: "Version v1.0.0"
  - type: doi
    value: 10.5281/zenodo.21811491
    description: "Version v1.1.0"
  # Resolved against the Zenodo API, not copied forward. The next two records are
  # each labelled one release behind the tree they archive, because .zenodo.json's
  # version field was bumped after tagging rather than in the tagged commit. The
  # label is permanent per record; what each ARCHIVES is the reliable field.
  - type: doi
    value: 10.5281/zenodo.21814982
    description: "Archives tree v1.2.0 (record labelled v1.1.0)"
  - type: doi
    value: 10.5281/zenodo.21816837
    description: "Archives tree v1.3.0 (record labelled v1.2.0)"
  - type: doi
    value: 10.5281/zenodo.21816871
    description: "Version v1.3.1 -- first record whose label matches its contents"
  # v1.4.0 has no record and never will. Its GitHub release fired at
  # 2026-08-06T16:28:41Z; the Zenodo webhook was created at 23:27:35Z the same day,
  # seven hours later, so nothing was listening. The hook's delivery log is empty and
  # Zenodo does not archive a release that predates its own hook. The tag stands in
  # git, and the tree it names is archived by the next record below rather than by one
  # of its own. This is why the list jumps: it is a gap in the record set, not an
  # omission from this file.
  - type: doi
    value: 10.5281/zenodo.22072086
    description: "Version v1.5.0 -- archives the v1.4.0 tree and everything since"
  - type: doi
    value: 10.5281/zenodo.22100841
    description: "Version v1.5.1 -- the 24 Aug disclosure re-query"
# verified against the live remote
repository-code: "https://github.com/shivamlalakiya/conformal-coverage-audit"
# This is what GitHub's cite widget and the indexers read. Left at v1.1.0 it sent a
# reviewer to the snapshot whose DISCLOSURE.md carries one row and an open TODO list,
# and the write-up's disclosure claim was judged against that. Bump it in the commit
# that gets tagged, not after -- a release naming the previous version is the same bug.
version: v1.5.1
date-released: "2026-08-25"
abstract: >-
  Measurement harnesses used to audit how Python conformal-prediction
  implementations resolve the conformal quantile at finite sample sizes. Each
  script re-derives its own formulas with `fractions.Fraction` the moment it
  loads, and each writes a plain-text output file that is committed alongside it.
  Covers the convention in isolation; the nine Hyndman-Fan quantile definitions
  read as carriers of a coverage guarantee; end-to-end runs through sktime,
  river, darts and statsforecast; paired real-data coverage on two Monash
  collections and two OpenML suites, in which the library arm and a
  required-rank arm are built from the same scores and the same point
  prediction; a census of the level-to-rank resolution sites across the audited
  packages, verified on disk; a classifier that takes an arbitrary quantile
  helper and reports what it delivers; a check of whether the mechanism
  generalises beyond conformal prediction to value-at-risk, tolerance bounds and
  bootstrap intervals; and a rebuttal probe that adjudicates a retracted finding
  of the author's own.
keywords:
  - conformal prediction
  - uncertainty quantification
  - finite-sample coverage
  - order statistics
  - prediction intervals
  - software correctness
  - reproducibility
