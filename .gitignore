# Environments and caches
.venv-*/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store

# Credentials — never commit
.env
.env.*
*.pem
*.key
*.p12
credentials.json

# ---------------------------------------------------------------------------
# HELD BACK ON PURPOSE — not redacted, not edited, just not committed yet.
# These two outputs print a finding that has not been reported upstream yet
# (see ARTIFACT.md section 1 at the Research root). Both regenerate exactly by
# running the probes, which ARE committed, so nothing here is unreproducible.
# Delete these two lines and commit the files once the upstream disclosure is
# made or the relevant issue is answered.
outputs/probe_output_sf_rebuttal.txt
outputs/probe_output_v5.txt
# ---------------------------------------------------------------------------

# Working documents and non-redistributable material — must never enter this
# repo, even if a stray copy is dropped in here
research-handoff.md
paper1-knowledge-base.md
publication-options.md
ARTIFACT.md
*.bak
cp-src/
superseded-probes/
scratchpad/
dewolf*

# work in progress, attribution not yet valid (see paper1-knowledge-base.md 8.1.6a)
probes/run_real_data.py
outputs/probe_output_real_data.txt
